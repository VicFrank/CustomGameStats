# @material-ui/core v4 only declares peer support for React <=17, but we run
# React 18 (works fine in practice). Ignore peer-dep conflicts so plain
# `npm install` works locally and during the Heroku heroku-postbuild step.
legacy-peer-deps=true
